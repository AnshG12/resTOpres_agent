# Core dependencies
httpx>=0.27           # HTTP client for API calls
python-dotenv>=1.0    # Environment variable management
pydantic>=2.6         # Data validation and parsing
rich>=13.7            # Terminal output formatting
typer>=0.12           # CLI framework

# External System Requirements (not Python packages):
# - latexmk: For PDF compilation (install via: brew install --cask mactex on macOS, apt-get install latexmk on Linux)
# - LaTeX distribution (TeX Live, MiKTeX, or MacTeX)
